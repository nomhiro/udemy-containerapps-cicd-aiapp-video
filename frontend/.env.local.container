# 開発ローカル用 (コミットしない)
# FastAPI (uvicorn) 実サーバ: compose 内部ポートを 80 に統一 (Container Apps と同じ)
NEXT_PUBLIC_API_BASE_URL=/api  # クライアントは相対で Next.js API を叩きサーバ側でプロキシ
# Compose 内のサービス間通信はコンテナ内部ポートを参照するため 80 を指定 (ホスト側 8000→80 マッピングとは無関係)
BACKEND_API_BASE=http://todo-backend:80