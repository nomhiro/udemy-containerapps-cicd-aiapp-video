# 開発ローカル用 (コミットしない)
# FastAPI (uvicorn) 実サーバ (port 8000) を利用
NEXT_PUBLIC_API_BASE_URL=/api  # クライアントは相対で Next.js API を叩きサーバ側でプロキシ
# IPv6 (::1) 解決で失敗する環境があるため 127.0.0.1 を明示
BACKEND_API_BASE=http://127.0.0.1:8000
