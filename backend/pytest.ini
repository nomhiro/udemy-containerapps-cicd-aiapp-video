[pytest]
pythonpath = src
markers =
	live_cosmos: Run tests that hit a real Cosmos DB instance (off by default)
filterwarnings =
	ignore::DeprecationWarning:httpx.*
	ignore::DeprecationWarning:fastapi.*
asyncio_mode = auto